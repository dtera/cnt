version: '3.3'

services:
  ngx:
    #build: .
    image: nginx
    container_name: ngx
    network_mode: host
    #ports:
    #    - '80:80'
    #    - '443:443'
    #volumes:
    #    - '/storage/docker/nginx/etc/nginx.conf:/etc/nginx/nginx.conf'
